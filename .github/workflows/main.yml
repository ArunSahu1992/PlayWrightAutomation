name: Playwright Manual Run (Linux Runner)

on:
  workflow_dispatch:   # 👈 MANUAL trigger only

jobs:
  run-playwright:
    runs-on: ubuntu-latest   # your Linux runner

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore & Build
        working-directory: SwatchBharatMission.Automation.Service
        run: |
          dotnet restore
          dotnet build --configuration Release

      - name: Install Playwright Browsers
        working-directory: SwatchBharatMission.Automation.Service/SwatchBharatMission.Automation.Service
        run: |
              chmod +x bin/Release/net8.0/playwright.sh
              bin/Release/net8.0/playwright.sh install --with-deps

      - name: Run Playwright Automation
        run: |
          dotnet run --configuration Release
